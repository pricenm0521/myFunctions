# main.py

# call(invoke) the function
import functions # brings in the functions module we have moved the function to
functions.print_even(10) # entry point, NOT PART OF THE FUNCTION, slight change in how we call the function

print(functions.convert("hello world"))
